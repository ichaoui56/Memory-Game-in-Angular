export interface AnimationEffect {
  color: string; // The color being animated
  duration: number; // Duration of the animation in milliseconds
  type: string; // Type of animation (e.g., "flash", "zoom")
}
